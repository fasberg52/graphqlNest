type User {
  id: Int!
  username: String!
  email: String!
}

type Query {
  users: [User!]!
  user(id: Int!): User
}

type Mutation {
  createUser(username: String!, email: String!): User!
  deleteUser(id: Int!): Boolean!
}
